http protocol "proxy" {
	return error
	pass
	match label "Prohibited!"
	block url "facebook.com/"
	block url "youtube.com/"
}

relay proxy {
	listen on $rly_IPv4 port 3128
        protocol "proxy"
        forward to nat lookup
}
